name: Deploy — Production

# Triggered manually or on merge to main
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  # -------------------------------------------------------
  # TODO: Replace these stubs with your actual deploy steps
  # Suggested targets:
  #   API  → Laravel Forge / Vapor / DigitalOcean App Platform
  #   Web  → Vercel (recommended for Next.js)
  # -------------------------------------------------------

  deploy-api:
    name: Deploy API
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Deploy Laravel API
        run: |
          echo "TODO: Add deployment steps for the Laravel API."
          echo "Examples:"
          echo "  - SSH + git pull + composer install + php artisan migrate"
          echo "  - Laravel Forge deployment trigger"
          echo "  - Laravel Vapor: vendor/bin/vapor deploy"
        # Example Forge webhook (uncomment and set secret):
        # run: curl -s "${{ secrets.FORGE_DEPLOY_WEBHOOK }}"

  deploy-web:
    name: Deploy Web (Next.js)
    runs-on: ubuntu-latest
    environment: production
    needs: deploy-api
    steps:
      - uses: actions/checkout@v4

      - name: Deploy Next.js PWA
        run: |
          echo "TODO: Add deployment steps for the Next.js PWA."
          echo "Recommended: Vercel CLI or Vercel GitHub integration."
        # Example Vercel deploy (uncomment and configure):
        # - uses: amondnet/vercel-action@v25
        #   with:
        #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
        #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        #     working-directory: web
        #     vercel-args: "--prod"
